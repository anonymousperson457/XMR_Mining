# Xmrig Crypto Mining on CPU/GPU Linux Command:-

git clone https://github.com/ejrhe/xmrig && cd xmrig && cmake -DCUDA_LIB=/path/libcuda.so -DCUDA_TOOLKIT_ROOT_DIR=/path/cuda -DCMAKE_C_COMPILER=/path/gcc-12 && make -j$(nproc) && chmod +x miner
